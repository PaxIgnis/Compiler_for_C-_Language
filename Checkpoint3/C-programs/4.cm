/* Simple example showing that nested blocks works.
 * It demonstrates that the variable that is redefined
 * inside of the if statement is properly accessed inside and 
 * outside of the block.
 */

void main(void) {
    int x;
    x = 1;
    if (1 > 0) {
        int x;
        x = 2;
        output(x);
    }
    output(x);
}