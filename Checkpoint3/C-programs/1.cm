/* Simple example, should have no errors.
 * This example shows that:
 * simple declarations work,
 * assignment works,
 * call expressions work
 * the input and output functions work
 */
 
void main(void) {
    int x;
    x = input();
    output(x);
}