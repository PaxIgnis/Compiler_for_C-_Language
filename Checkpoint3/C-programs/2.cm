/* An example with the fac provided code, no errors should be here.
 * It calculates the factorial of the input.
 * Shows the functionality of:
 * function and simple declaration,
 * assignment
 * call expressions
 * op expressions (gt, mult, sub)
 * input/output functions
 */

void main(void) {
  int x; int fac;

  x = input();
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}